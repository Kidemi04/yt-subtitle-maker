/**
 * @yt-subtitle-maker/api-client
 *
 * HTTP / NDJSON client + shared types for the v2.0 backend contract.
 * Used by apps/desktop today; apps/mobile (V2) will reuse identically.
 */
export * from "./types";
export { ApiClient } from "./client";
